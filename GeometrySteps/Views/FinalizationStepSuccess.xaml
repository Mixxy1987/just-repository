<UserControl x:Class="GeometrySteps.Views.FinalizationStepSuccess"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewModels="clr-namespace:GeometrySteps.ViewModels"
             xmlns:properties="clr-namespace:GeometrySteps.Properties"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance d:Type=viewModels:FinalizationStepViewModel}"
             d:DesignHeight="800" Background="White">
    <Grid>
        <Grid.Resources>
            <ResourceDictionary
                Source="../Styles/Styles.xaml" />
        </Grid.Resources>
        <StackPanel Orientation="Vertical">
            <TextBlock Margin="{StaticResource H2Margin}" Style="{StaticResource H2}"
                       Text="{x:Static properties:Resources.Final_Title}" />
        </StackPanel>
    </Grid>
</UserControl>